package com.workintech.Banking.service;

import org.springframework.boot.autoconfigure.amqp.RabbitConnectionDetails;

public class AdressService {
    List<Adress> findAll();
    Address find(int id);
    Adress save(Customer)
}
